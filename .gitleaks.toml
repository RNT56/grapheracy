title = "Graphview secret scanning policy"

[extend]
useDefault = true

[allowlist]
description = "Documented local-only placeholders in .env.example"
paths = ['''^\.env\.example$''']
regexes = ['''graphview-local-only|local-only''']
