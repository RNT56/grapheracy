title = "Graphview secret scanning policy"

[extend]
useDefault = true

[allowlist]
description = "Documented local placeholders and encrypted runtime ciphertext"
paths = ['''^\.env\.example$''', '''^\.graphview/objects/.*\.gvenc$''']
regexes = ['''graphview-local-only|local-only''']
