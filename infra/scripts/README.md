# Infrastructure Scripts

Operational scripts belong here once Phase 2 adds runnable services.

Phase 1 validation scripts live in the root `scripts/` directory because they are repository hygiene checks rather than
deployment automation.
