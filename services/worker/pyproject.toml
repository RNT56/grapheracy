[project]
name = "graphview-worker"
version = "0.0.0"
description = "Future async ingestion worker for Graphview."
requires-python = ">=3.14"
dependencies = [
  "arq",
  "pydantic-settings",
  "pypdf"
]

[dependency-groups]
dev = [
  "pytest"
]
