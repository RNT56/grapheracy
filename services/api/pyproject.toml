[project]
name = "graphview-api"
version = "0.0.0"
description = "Future FastAPI service for Graphview."
requires-python = ">=3.14"
dependencies = [
  "alembic",
  "fastapi",
  "pydantic-settings",
  "sqlalchemy",
  "uvicorn"
]

[dependency-groups]
dev = [
  "httpx",
  "pytest"
]
