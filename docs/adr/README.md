# Architecture Decision Records

ADRs are one decision per file using `YYYY-MM-DD-short-title.md`.

Use `template.md` for new decisions. Add an ADR when a dependency or platform choice becomes architectural.
