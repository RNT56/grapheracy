---
type: added
owner: codex
---

# Phase 27 active agent context connectors

- Add scoped active-context capture contracts, database tables, migration, adapter-token auth, redacted encrypted blobs,
  context retention, backup metadata, REST/SSE routes, graph projection, and activity integration.
- Add the local agent gateway with MCP-compatible tool manifests, Codex/Claude wrapper entrypoints, path denial,
  redaction, checksums, and offline outbox retry behavior.
- Add a VS Code/Cursor-compatible extension scaffold for editor visibility, selection, save, workspace metadata, and
  heartbeat observations.
- Add the Active Context web lens with session selection, timeline, authority badges, content state indicators, graph
  projection, and live-update route wiring.
- Add worker enrichment/retention stages, documentation, release checklist coverage, and `phase27:check`.
